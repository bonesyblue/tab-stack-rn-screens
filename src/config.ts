// Feature flag: Controls whether the app makes use of react-native-screens
export const USE_SCREENS = true;

// Feature flag: Controls whether the app should make network requests
export const MAKE_NETWORK_REQUESTS = true;
